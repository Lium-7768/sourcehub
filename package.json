{
  "name": "sourcehub",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "cf-typegen": "wrangler types",
    "check": "tsc --noEmit",
    "test": "tsx tests/source-validation.test.ts && tsx tests/adapter-normalization.test.ts && tsx tests/measurements-api.test.ts && tsx tests/probe-service.test.ts",
    "smoke": "node scripts/smoke-check.mjs"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250314.0",
    "tsx": "^4.20.5",
    "typescript": "^5.8.2",
    "wrangler": "^4.7.1"
  }
}
