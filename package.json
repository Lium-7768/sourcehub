{
  "name": "sourcehub",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "cf-typegen": "wrangler types",
    "check": "tsc --noEmit",
    "smoke": "node scripts/smoke-check.mjs",
    "refresh:public-results": "node scripts/refresh_public_results_db.mjs",
    "pipeline:public-results": "node scripts/run_probe_and_refresh.mjs"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250314.0",
    "typescript": "^5.8.2",
    "wrangler": "^4.7.1"
  }
}
